// mockContent.ts
// 测试用的正文/思考过程样本。覆盖会引发抖动的典型 markdown 结构：
// 标题、加粗、有序/无序列表、表格（流式中「半成品 -> 突然成表」跳变）、代码块、分隔线。

export interface Scenario {
  key: string;
  label: string;
  content: string;
}

// 场景 1：线上数据 - COS 相关（对应参考图）
const cosContent = `根据搜索结果，动漫角色 COS 的热度主要集中在《王者荣耀》游戏角色，其中以下角色最受欢迎：

### 一、王者荣耀人气角色 COS

1. **西施**
   诗语江南、游龙清影等皮肤造型备受青睐，用户多次发布相关 COS 作品，强调"萌萌哒"的风格和精致服饰细节。皮肤搭配蓝白渐变搭配和江南风服饰，突出角色灵动感。

2. **公孙离**
   以"蜜橘之夏""玉兔公主"等皮肤为主，COS 注重还原兔耳头饰和飘逸裙摆。用户提出多份室内外场景图，兔子白发角色搭配花朵装饰的造型获高赞点赞。

3. **貂蝉**
   "仲夏夜之梦""异色猫娘"等皮肤是热门选择，粉发与蕾丝元素结合的造型讨论度高。

### 二、其它高热度 IP

| 角色 | 出处 | 热度指数 | 代表皮肤 |
| --- | --- | --- | --- |
| 甘雨 | 原神 | 9.2 | 麒麟 |
| 雷电将军 | 原神 | 8.9 | 雷电 |
| 阿尼亚 | 间谍过家家 | 8.5 | 校服 |

### 三、拍摄建议

- **光线**：优先自然光，逆光可营造氛围感
- **场景**：室内可用纯色背景板，室外推荐樱花、竹林
- **后期**：注意肤色统一，避免过曝

> 提示：以上数据来源于近 30 天社区发布量统计，仅供参考。

---

综上，若追求热度与还原度，推荐优先考虑*王者荣耀*系列角色。`;

// 场景 2：投研分析（含大量列表 + 表格 + 数字）
const financeContent = `帮你分析一下这只基金的近期表现。

## 一、业绩概览

该基金近一年收益率为 **+12.35%**，同期业绩比较基准为 +8.20%，超额收益约 4.15%。

### 阶段涨跌幅

| 区间 | 本基金 | 基准 | 超额 |
| --- | --- | --- | --- |
| 近一月 | +2.10% | +1.50% | +0.60% |
| 近三月 | +5.80% | +4.20% | +1.60% |
| 近一年 | +12.35% | +8.20% | +4.15% |
| 成立以来 | +45.60% | +30.10% | +15.50% |

## 二、持仓分析

1. **行业配置**：前三大行业为电子（22%）、医药（18%）、新能源（15%）
2. **集中度**：前十大重仓股占比 52%，集中度中等偏高
3. **换手率**：年化换手率约 180%，属于积极管理型

## 三、风险提示

- 最大回撤 **-15.20%**，回撤修复用时约 4 个月
- 夏普比率 1.35，风险调整后收益良好

> 结论：该基金进攻性较强，适合风险承受能力较高的投资者中长期持有。`;

// 场景 3：代码讲解（含代码块，流式中代码块闭合前后抖动明显）
const codeContent = `这个问题可以用防抖来解决，下面给你一个实现：

## 实现思路

我们需要在流式更新时**合并高频调用**，用节流 + rAF 对齐帧。

\`\`\`javascript
function rafThrottle(fn) {
  let scheduled = false;
  let lastArgs;
  return function (...args) {
    lastArgs = args;
    if (scheduled) return;
    scheduled = true;
    requestAnimationFrame(() => {
      scheduled = false;
      fn.apply(this, lastArgs);
    });
  };
}
\`\`\`

## 关键点

1. 每帧最多执行一次，避免同一帧内多次重排
2. 保留最后一次参数，保证最终状态正确
3. 配合增量解析，只重解析最后一个未闭合块

这样就能显著减少布局抖动了。`;

export const SCENARIOS: Scenario[] = [
  { key: 'cos', label: '线上数据 - COS 相关', content: cosContent },
  { key: 'finance', label: '投研分析 - 基金', content: financeContent },
  { key: 'code', label: '代码讲解 - 含代码块', content: codeContent },
];

// 思考过程样本
export const THINKING_SAMPLES: Record<string, { key: string; label: string; content: string }> = {
  none: { key: 'none', label: '无思考过程', content: '' },
  short: {
    key: 'short',
    label: '简短思考',
    content: '用户想了解动漫角色 COS 的热度排行。我需要检索社区数据，按发布量和点赞量综合排序，再给出拍摄建议。',
  },
  long: {
    key: 'long',
    label: '较长思考',
    content: `让我梳理一下这个问题的分析路径：

1. 首先确定数据来源：社区近 30 天的发布量、点赞量、评论量。
2. 其次做角色聚类：按 IP 分组，统计每个角色的作品数。
3. 然后计算热度指数：发布量 × 0.4 + 点赞 × 0.4 + 评论 × 0.2，归一化到 10 分制。
4. 排序后取 TOP，补充皮肤/造型的具体信息。
5. 最后给出可执行的拍摄建议，覆盖光线、场景、后期三个维度。

这样输出结构清晰，用户可以直接参考。`,
  },
};

export function getScenario(key: string): Scenario {
  return SCENARIOS.find(s => s.key === key) || SCENARIOS[0];
}
